# @todo ensure xsdk fails if not support properties
project
  : default-build
    <toolset>gcc-arm7

    <target-os>elf

    <architecture>arm
    <instruction-set>armv7

    <link>static
  ;

build-project configure ;
build-project standard-generator ;
